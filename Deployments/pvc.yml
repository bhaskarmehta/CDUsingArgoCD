apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc3
  namespace: default
spec:
  storageClassName: "my-sc"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Gi