apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc2
  namespace: default
spec:
  storageClassName: "gp2"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Gi