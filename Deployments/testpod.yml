kind: Pod 
apiVersion: v1 
metadata: 
   name: testpod 
spec: 
   containers: 
      - name: nginx 
        image: nginx
        ports:
          - containerPort: 80
        volumeMounts: 
           - name: myvol
             mountPath: /temp/data
   volumes: 
      persistentVolumeClaim: 
         claimName: my-pvc               